name: CI

on:
  # Triggers the workflow on pull request for the main branch
  push:
    branches: [main, stable]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Run DepCtrl manifest verification
  verify-depctrl:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Validate DepCtrl manifest
        run: python validate_depctrl.py
